# Copy to .env for `docker compose up`. These are the local Postgres credentials.
POSTGRES_DB=scholarshub
POSTGRES_USER=scholars
POSTGRES_PASSWORD=scholars_pw

# Where the browser reaches the API and which origin the API allows.
VITE_API_URL=http://localhost:8080/api
FRONTEND_ORIGIN=http://localhost:8081
